import { FC } from "react";

import { SectionBox } from "../../elements/SectionBox.tsx";

export const EducationSection: FC = () => {
    return (
        <SectionBox>
            <div></div>
        </SectionBox>;
    );
};
